[build]
  base = "."
  command = "npm run build --workspace hillside-next"

[build.environment]
  NODE_VERSION = "20"
  NPM_FLAGS = "--include=optional"
  NPM_CONFIG_OPTIONAL = "true"

[[plugins]]
  package = "@netlify/plugin-nextjs"
