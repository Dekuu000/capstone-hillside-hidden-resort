[build]
  # Legacy React app is archived; Netlify config now targets hillside-next.
  base = "hillside-next"
  command = "npm run build"
  publish = ".next"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
