# Admin Dashboard Override

- Layout pattern: KPI grid + queue widgets + quick actions
- Primary CTA: `Review reservations` in header actions
- Density: high (data-dense)
- Key components: `AppShell`, `PageHeader`, `Badge`, `Card`, `Skeleton`
- Loading behavior: KPI skeleton cards
- Empty behavior: empty queue widgets with links
- Error behavior: top banner with retry button
- Offline behavior: show stale data timestamp chip
