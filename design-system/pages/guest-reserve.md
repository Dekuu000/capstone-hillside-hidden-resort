# Guest Reserve Override

- Layout pattern: progressive form + sticky summary card
- Primary CTA: `Continue`/`Reserve now` anchored near summary
- Density: medium
- Key components: `Input`, `Button`, `Badge`, `Skeleton`, `Toast`
- Loading behavior: unit list skeletons and disabled submit during quote
- Empty behavior: no units available state with date reset action
- Error behavior: inline alert + retry button
- Offline behavior: show offline queue status and encrypted local save indicator
