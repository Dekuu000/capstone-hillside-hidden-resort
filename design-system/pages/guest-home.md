# Guest Home Override

- Layout pattern: welcome header + upcoming stay card + quick actions
- Primary CTA: `Reserve` on top card
- Density: low-medium
- Key components: `GuestShell`, `PageHeader`, `Card`, `Badge`, `EmptyState`
- Loading behavior: card skeletons
- Empty behavior: no upcoming stay -> empty state with reserve CTA
- Error behavior: retry banner with concise message
- Offline behavior: show last synced timestamp badge
